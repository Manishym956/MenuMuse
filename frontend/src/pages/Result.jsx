import React from 'react';
import ResultCard from '../components/ResultCard';

const Result = () => (
  <div style={{ textAlign: 'center', marginTop: '4rem' }}>
    <ResultCard dish={null} reason={null} />
  </div>
);

export default Result; 